# Screenshots

Drop real in-game screenshots here (PNG/JPG) and reference them from
`docs/index.html`'s "Interface" section in place of (or alongside) the CSS
mockup panel. The mockup is a fair approximation of the real ClickGUI's
look — since it's built from the same `Theme.java` colors — but nothing
beats an actual screenshot for a landing page.
